Introducing an indented run of backticks.

    ```
    still inside the indented chunk
Ordinary prose that must stay translatable.

And a tilde spelling of the same shape.

    ~~~
    still inside the indented chunk
More ordinary prose after it.

Three columns is still a fence, and it is closed.

   ```
   held in the skeleton
   ```

- An item whose fence is indented into it.

  ```
  also held in the skeleton
  ```

- An item whose unclosed fence ends where the item does.

      ```
    ```

Prose after the list.
