# A source that spells ⟦1⟧

The pipeline's own token is ⟦1⟧, written here as ordinary prose, and again as
⟦12⟧ inside a wrapped paragraph so the run reaches a second line.

A code span holds one: `⟦2⟧`. So does a longer one: `⟦2⟧bold⟦1⟧`.

A link destination is [the token](⟦3⟧) and a reference is [style][⟦4⟧].

Inline HTML carries one in an attribute: <span title="⟦5⟧">marked</span>.

Adjacent tokens ⟦6⟧⟦7⟧, a wide id ⟦987654321⟧, and a zero ⟦0⟧.

An unpaired bracket ⟦8 and 9⟧ is not a token and stays prose.

    An indented code block with ⟦10⟧ is skeleton.

```text
A fenced block with ⟦11⟧ is skeleton too.
```

> A quoted line with ⟦13⟧.

| Head | ⟦14⟧ |
| --- | --- |
| a | ⟦15⟧ |

[⟦4⟧]: https://example.com/ref
